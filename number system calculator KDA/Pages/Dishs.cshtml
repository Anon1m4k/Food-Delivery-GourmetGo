@page
@model number_system_calculator_KDA.Pages.DishsModel
@{
}

<h2>Menu</h2>

<p>
    <a class="btn btn-primary" href="@Url.Page("Menu", new { id = 0 })">Добавить новое блюдо</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Structure</th>
            <th>Gram</th>
            <th>Price</th>            
        </tr>
    </thead>
    <tbody>
        @foreach (var dish in Model.Dishs)
        {
            <tr>
                <td>@dish.Title</td>
                <td>@dish.Structure</td>
                <td>@dish.Gram</td>
                <td>@dish.Price</td>

                <td>
                    <a href="@Url.Page("Menu", new { id = dish.Id })">Edit</a> |
                    <a href="javascript:void(0);" onclick="if (confirm('Are you sure you want to delete?')) { window.location.href = '@Url.Page("BookDelete", new { id = dish.Id })'; }">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>